# 19. Метрики и функции потерь в сегментации

## Pixel-wise CE, IoU/mIoU, Dice/F1, Weighted CE, Focal loss, Tversky

---

## 1. Постановка

В задачах сегментации у нас есть:
- **Prediction**: $\hat{y} \in \mathbb{R}^{H \times W \times K}$ (logits)
- **Ground truth**: $y \in \{0,1\}^{H \times W \times K}$ (one-hot) или $y \in \{0,\ldots,K-1\}^{H \times W}$ (индекс класса)
- $N = H \times W$ — количество пикселей
- $K$ — количество классов

### Два типа метрик

| Тип | Цель | Примеры |
|-----|------|---------|
| **Loss functions** | Используются для обучения (требуют дифференцируемости) | CE, Dice, Focal, Tversky |
| **Evaluation metrics** | Используются для оценки (не обязательно дифференцируемы) | IoU, mIoU, Dice, Accuracy |

---

## 2. Pixel-wise Cross-Entropy (CE)

### Формула

**Для бинарной:**
$$ \mathcal{L}_{BCE} = -\frac{1}{N} \sum_{i=1}^{N} [y_i \log(\hat{y}_i) + (1 - y_i) \log(1 - \hat{y}_i)] $$

**Для многоклассовой:**
$$ \mathcal{L}_{CE} = -\frac{1}{N} \sum_{i=1}^{N} \sum_{k=1}^{K} y_{i,k} \log(\hat{y}_{i,k}) $$

Где $\hat{y}_{i,k} = \text{softmax}(z_{i,k}) = \frac{e^{z_{i,k}}}{\sum_j e^{z_{i,j}}}$

### Свойства

| Характеристика | Значение |
|----------------|----------|
| **Конвексность** | Выпукла (по softmax) |
| **Градиент** | $\frac{\partial \mathcal{L}}{\partial z_k} = \hat{y}_k - y_k$ (элегантно!) |
| **Чувствительность** | Равная ко всем пикселям |
| **Дисбаланс классов** | ❌ Плохо (disfavours rare classes) |
| **Дифференцируемость** | ✅ Да |

### Проблема: дисбаланс классов
В [[DL 16 - Семантическая сегментация|сегментации]] часто:
- Фон — 95% пикселей
- Объект — 5%
- CE будет доминироваться фоном → модель игнорирует объект

---

## 3. Weighted Cross-Entropy (WCE)

### Идея
Присвоить вес каждому классу, пропорционально его редкости.

### Формула

$$ \mathcal{L}_{WCE} = -\frac{1}{N} \sum_{i=1}^{N} \sum_{k=1}^{K} w_k \cdot y_{i,k} \log(\hat{y}_{i,k}) $$

### Варианты весов

1. **Inverse class frequency**: $w_k = \frac{N}{K \cdot N_k}$
   - $N_k$ — количество пикселей класса $k$ в датасете

2. **Median frequency balancing**: $w_k = \frac{\text{median}(\{N_1,\ldots,N_K\})}{N_k}$

3. **Square root inverse**: $w_k = \sqrt{\frac{N}{K \cdot N_k}}$

### Плюсы и минусы

| Плюсы | Минусы |
|-------|--------|
| ✅ Простая модификация CE | ❌ Веса нужно подбирать |
| ✅ Решает дисбаланс | ❌ Может привести к нестабильности |
| ✅ Работает с любыми K | ❌ Не учитывает пространственную структуру |

---

## 4. Focal Loss

### Идея (Lin et al., 2017 для RetinaNet)
Уменьшить вклад **хорошо классифицированных** примеров в loss. Фокус на «тяжёлых» пикселях.

### Формула

$$ \mathcal{L}_{focal} = -\frac{1}{N} \sum_{i=1}^{N} \sum_{k=1}^{K} y_{i,k} (1 - p_{i,k})^\gamma \log(p_{i,k}) $$

Где:
- $p_{i,k} = \hat{y}_{i,k}$ — вероятность правильного класса
- $\gamma \geq 0$ — focusing parameter (обычно 2)
- $(1-p)^\gamma$ — modulating factor

### Анализ

$$ p \to 1 \text{ (лёгкий пример)} \rightarrow (1-p)^\gamma \to 0 \text{ (вклад ≈ 0)} $$
$$ p \to 0 \text{ (тяжёлый пример)} \rightarrow (1-p)^\gamma \to 1 \text{ (вклад ≈ CE)} $$

### Сравнение CE и Focal

```
Loss(p)
│
│   CE: -log(p)
│   Focal γ=2: -(1-p)²·log(p)
│
│   Тяжёлые: одинаковы
│   Лёгкие: Focal ≈ 0, CE > 0
│
└───────────────────────→ p
0                      1
```

### Варианты

| Версия | Формула | Комментарий |
|--------|---------|-------------|
| CE | $-\log(p)$ | Базовый |
| Focal (γ=2) | $-(1-p)^2 \log(p)$ | Стандартный |
| Asymmetric Focal | $-(\alpha)(1-p)^{\gamma_1} \log(p)$ | Разные γ для pos/neg |
| Weighted Focal | $-w_k(1-p)^\gamma \log(p)$ | Focal + class weights |

---

## 5. IoU (Intersection over Union) / Jaccard Index

### Определение

$$ \text{IoU} = \frac{|A \cap B|}{|A \cup B|} = \frac{TP}{TP + FP + FN} $$

- $A$ — ground truth маска
- $B$ — предсказанная маска
- IoU ∈ [0, 1]

### Для каждого класса

$$ \text{IoU}_k = \frac{\sum_i y_{i,k} \hat{y}_{i,k}}{\sum_i y_{i,k} + \sum_i \hat{y}_{i,k} - \sum_i y_{i,k} \hat{y}_{i,k}} $$

### Mean IoU (mIoU)

$$ \text{mIoU} = \frac{1}{K} \sum_{k=1}^{K} \text{IoU}_k $$

- Усреднение по всем классам
- Каждый класс имеет равный вес (даже редкие)
- Стандартная метрика оценки в сегментации

### IoU loss (дифференцируемый)

Для обучения нужно дифференцируемое приближение:

$$ \mathcal{L}_{IoU} = 1 - \frac{\sum_i y_i \hat{y}_i}{\sum_i y_i + \sum_i \hat{y}_i - \sum_i y_i \hat{y}_i + \epsilon} $$

Где $\hat{y}_i$ — sigmoid/softmax выход (не бинарный).

---

## 6. Dice Coefficient / F1 Score

### Определение

$$ \text{Dice} = \frac{2|A \cap B|}{|A| + |B|} = \frac{2TP}{2TP + FP + FN} $$

### Связь с IoU

$$ \text{Dice} = \frac{2 \cdot \text{IoU}}{1 + \text{IoU}} $$
$$ \text{IoU} = \frac{\text{Dice}}{2 - \text{Dice}} $$

| IoU | Dice |
|-----|------|
| 0.5 | 0.667 |
| 0.7 | 0.824 |
| 0.9 | 0.947 |
| 1.0 | 1.0 |

Dice всегда ≥ IoU для одного набора.

### Dice Loss

$$ \mathcal{L}_{Dice} = 1 - \frac{2\sum_i y_i \hat{y}_i + \epsilon}{\sum_i y_i + \sum_i \hat{y}_i + \epsilon} $$

Гладкие варианты:

1. **Sørensen-Dice**: $+ \epsilon$ (1e-7)
2. **Smooth Dice**: $\frac{2\sum y\hat{y} + 1}{\sum y + \sum \hat{y} + 1}$
3. **Log-Dice**: $-\log(\text{Dice})$

### Свойства Dice Loss

| Характеристика | Описание |
|----------------|----------|
| **Градиент** | $\frac{\partial \mathcal{L}}{\partial \hat{y}} = \frac{-2y(\sum y + \sum \hat{y}) + 2\sum y\hat{y}}{(\sum y + \sum \hat{y})^2}$ |
| **Дисбаланс** | ✅ Хорошо (пропорция, не абсолют) |
| **Гладкая граница** | ✅ (hard samples have non-zero gradient) |
| **Пространственная** | ❌ Не учитывает расстояния |
| **Пиксельная независимость** | ❌ Зависит от всех пикселей (глобальная метрика) |

---

## 7. Tversky Loss

### Идея
Обобщение Dice с разными весами для FP и FN.

### Формула

$$ \mathcal{L}_{Tversky} = 1 - \frac{\sum_i y_i \hat{y}_i + \epsilon}{\sum_i y_i \hat{y}_i + \alpha \sum_i (1-y_i)\hat{y}_i + \beta \sum_i y_i(1-\hat{y}_i) + \epsilon} $$

Где:
- $\alpha$ — вес для False Positives (FP)
- $\beta$ — вес для False Negatives (FN)

### Частные случаи

| Loss | $\alpha$ | $\beta$ | Примечание |
|------|----------|---------|------------|
| **Dice** | 0.5 | 0.5 | Равновесие FP/FN |
| **Tversky (FP focus)** | 1.0 | 0.0 | Наказывает перепредсказание |
| **Tversky (FN focus)** | 0.0 | 1.0 | Наказывает недопредсказание |
| **Focal Tversky** | 0.7 | 0.3 | Медицина (пропуск → высокая цена) |

### Focal Tversky Loss

$$ \mathcal{L}_{FTL} = (1 - \text{Tversky Index})^{\frac{1}{\gamma}} $$

С такими же $\alpha, \beta$, параметр $\gamma$ фокусирует на трудно классифицируемых.

---

## 8. Сравнительная таблица loss функций

| Loss | Тип | Дисбаланс | Градиент | IoU-ориентирован | Применение |
|------|-----|-----------|----------|------------------|------------|
| **CE** | Пиксельная | ❌ Плохо | Линейный | Нет | Общий baseline |
| **WCE** | Пиксельная+веса | ✅ Хорошо | Линейный | Нет | Медицина |
| **Focal** | Пиксельная | ✅ Отлично | Modulated | Нет | Маленькие объекты |
| **Dice** | Global | ✅ Хорошо | Ступенчатый | Да | Медицина, классы |
| **IoU** | Global | ✅ Хорошо | Ступенчатый | Да | Сегментация |
| **Tversky** | Global | ✅ Контролируемый | Ступенчатый | Да | Асимметрия FP/FN |
| **Combo (CE+Dice)** | Mixed | ✅ | Смешанный | Да | Практика |

---

## 9. Комбинация loss функций

На практике лучшие результаты — **комбинация** потерь:

$$ \mathcal{L} = \lambda_1 \mathcal{L}_{CE} + \lambda_2 \mathcal{L}_{Dice} $$

### Почему CE + Dice?

| Компонент | Роль |
|-----------|------|
| **CE** | Плавный градиент, стабильность, ранняя сходимость |
| **Dice** | Прямая оптимизация метрики, лучше при дисбалансе |

- Типичные веса: $\lambda_1 = 0.5$, $\lambda_2 = 0.5$
- Или $\mathcal{L} = \mathcal{L}_{Dice} + 0.3 \cdot \mathcal{L}_{Focal}$ (медицина)

---

## 10. Эффект разных loss на обучение

```
Dice Loss:
  + Прямая оптимизация метрики
  + Хороша для несбалансированных масок
  - "Резкие" градиенты
  - Неустойчива на старте

CE Loss:
  + Устойчива, гладкая
  + Хорошо на ранних эпохах
  - Игнорирует структуру маски

Focal Loss:
  + Фокус на границах и сложных пикселях
  + Дисбаланс мягко контролируется
  - Дополнительный гиперпараметр (γ)
```

---

## 11. Метрики оценки (не для обучения)

| Метрика | Формула | Особенности |
|---------|---------|-------------|
| **Pixel Accuracy** | $\frac{TP + TN}{TP + TN + FP + FN}$ | ❌ Плохо при дисбалансе |
| **Mean Accuracy** | $\frac{1}{K}\sum_k \frac{TP_k}{TP_k + FN_k}$ | Per-class |
| **IoU** | $\frac{TP}{TP+FP+FN}$ | ✅ Стандарт сегментации |
| **mIoU** | $\frac{1}{K}\sum_k \text{IoU}_k$ | ✅ Основная метрика |
| **Dice (F1)** | $\frac{2TP}{2TP+FP+FN}$ | ✅ Взаимозаменяема с IoU |
| **Precision** | $\frac{TP}{TP+FP}$ | Для отдельных классов |
| **Recall** | $\frac{TP}{TP+FN}$ | Для отдельных классов |

---

## Ключевые выводы к экзамену

1. **CE** — базовая loss, но плоха при дисбалансе классов
2. **Weighted CE** — веса для редких классов, решает дисбаланс
3. **Focal loss** — $(1-p)^\gamma \log(p)$, фокус на сложных пикселях. $\gamma=2$ типично
4. **IoU** — $\frac{TP}{TP+FP+FN}$, **mIoU** — mean по классам, стандартная метрика
5. **Dice** — $\frac{2TP}{2TP+FP+FN}$, связан с IoU через $\frac{2\cdot IoU}{1+IoU}$
6. **Tversky** — обобщение Dice с раздельными весами $\alpha, \beta$ для FP и FN
7. **Лучшая практика**: CE + Dice комбинация (стабильность + прямая оптимизация метрики)

Метрики сегментации частично пересекаются с метриками детекции — см. [[DL 21 - Метрики и NMS в детекции]].

---

**Связанные вопросы:**
- [[DL 16 - Семантическая сегментация]] — применение метрик к задаче сегментации
- [[DL 21 - Метрики и NMS в детекции]] — метрики детекции (AP/mAP)
