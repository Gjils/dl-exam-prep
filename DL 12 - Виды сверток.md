# DL 12 — Виды свёрток

## 1. Обзор

Стандартная 2D-свёртка — лишь один из множества вариантов. Разные виды свёрток решают разные задачи: уменьшение параметров, увеличение receptive field, изменение разрешения, разделение каналов.

---

## 2. 1×1 Convolution (Pointwise Convolution)

Свёртка с ядром $1 \times 1$:

$$y[i,j,c_{\text{out}}] = \sum_{c=0}^{C_{\text{in}}-1} x[i,j,c] \cdot w[c, c_{\text{out}}] + b$$

### Что делает:
- **Смешивает каналы**, не трогая пространственную структуру
- Изменяет число каналов ($C_{\text{in}} \to C_{\text{out}}$)
- Добавляет нелинейность (ReLU после)

### Применения:

| Применение | Суть |
|---|---|
| **Bottleneck** | Сократить каналы $C \to C/4$ → обратно $C$ ([[DL 14 - ResNet|ResNet]] bottleneck) |
| **Channel expansion** | Увеличить каналы перед большим ядром |
| **Depthwise separable** | Первый шаг (depthwise) + 1×1 (pointwise) |
| **Network-in-Network** | Добавление ёмкости без изменения размера |

### Пример (bottleneck в [[DL 14 - ResNet|ResNet]]):

```python
# ResNet bottleneck block
x → Conv 1×1 (C → C/4) → Conv 3×3 (C/4 → C/4) → Conv 1×1 (C/4 → C) → + skip
```

### Параметры:
$1 \times 1 \times C_{\text{in}} \times C_{\text{out}} + C_{\text{out}}$ — **только смешивание каналов**, без пространственного.

---

## 3. Depthwise Separable Convolution

Разделяет свёртку на два этапа:

### 3.1. Depthwise Convolution (пространственная)
Каждый канал сворачивается **независимо**:

$$\text{Depthwise: } C_{\text{in}} \times (k \times k) \text{ параметров}$$

$$\text{Output: } H \times W \times C_{\text{in}}$$

### 3.2. Pointwise Convolution (канальная)
1×1 свёртка для смешивания каналов:

$$\text{Pointwise: } C_{\text{in}} \times C_{\text{out}} \text{ параметров}$$

### Сравнение параметров (стандартная vs separable):

**Стандартная Conv2D**:
$$\text{Params} = k \cdot k \cdot C_{\text{in}} \cdot C_{\text{out}}$$

**Depthwise Separable**:
$$\text{Params} = k \cdot k \cdot C_{\text{in}} + C_{\text{in}} \cdot C_{\text{out}}$$

**Экономия**:

$$\text{Ratio} = \frac{k^2 C_{\text{in}} + C_{\text{in}} C_{\text{out}}}{k^2 C_{\text{in}} C_{\text{out}}} \approx \frac{1}{k^2} + \frac{1}{C_{\text{out}}} \approx \frac{1}{k^2}$$

Для $k=3$, $C_{\text{out}}$ большое: **~9× меньше параметров!**

### Где используется:
- **MobileNet** (мобильные устройства, см. [[DL 11 - Базовая CNN]])
- **Xception**
- **EfficientNet**
- **ConvNeXt**

```python
class DepthwiseSeparableConv(nn.Module):
    def __init__(self, in_ch, out_ch, kernel=3):
        super().__init__()
        self.depthwise = nn.Conv2d(in_ch, in_ch, kernel, 
                                    padding=kernel//2, groups=in_ch)
        self.pointwise = nn.Conv2d(in_ch, out_ch, 1)
    
    def forward(self, x):
        return self.pointwise(self.depthwise(x))
```

---

## 4. Transposed Convolution (Deconvolution)

**Повышает разрешение** (upsampling) — обратная операция к свёртке в смысле формы, **но не математически обратная**.

### Как работает:
- Вход: $H \times W$
- Выход: $H' > H$, $W' > W$
- Каждый входной элемент "распределяется" по ядру

### Размер выхода (для $H_{\text{out}} = s \cdot H$):

$$H_{\text{out}} = (H - 1) \cdot s - 2p + k$$

### Иллюстрация ($k=3$, $s=2$):

```
Вход 2×2:
┌───┬───┐
│ 1 │ 2 │
├───┼───┤
│ 3 │ 4 │
└───┴───┘

Transposed Conv (3×3, s=2) → 5×5:
Каждый элемент 1,2,3,4 "размазывается" ядром 3×3
с перекрытиями → 5×5 выход
```

### Применения:
- **Semantic Segmentation** ([[DL 17 - U-Net|U-Net]], Feature Pyramid Networks)
- **Generative Models** (GANs, VAE)
- **Super-resolution**
- **Image-to-Image translation**

### Проблема: Checkerboard Artifacts
Transposed conv может давать "шахматные" артефакты. **Решение**: использовать **Upsample + Conv** вместо Transposed Conv:

```python
# Вместо nn.ConvTranspose2d
x = nn.Upsample(scale_factor=2, mode='bilinear', align_corners=False)(x)
x = nn.Conv2d(C, C, 3, padding=1)(x)  # сгладить артефакты
```

---

## 5. Dilated (Atrous) Convolution

Свёртка с промежутками (dilation):

$$y[i,j] = \sum_{m=0}^{k-1} \sum_{n=0}^{k-1} x[i + d \cdot m, j + d \cdot n] \cdot w[m,n]$$

### Эффективное рецептивное поле:

$$k_{\text{eff}} = k + (k-1)(d-1)$$

| Dilation | Kernel | Эффективный размер | Параметров |
|---|---|---|---|
| d=1 | 3×3 | 3×3 | 9 |
| d=2 | 3×3 | 5×5 | 9 |
| d=3 | 3×3 | 7×7 | 9 |
| d=4 | 3×3 | 9×9 | 9 |

**Рост receptive field без увеличения параметров!**

### Применения:
- **Semantic Segmentation** (DeepLab v1-v3): большое RF без потери разрешения
- **WaveNet** (аудио): fast-growing receptive field
- **Multi-scale context**: разные dilation в параллельных ветках (ASPP)

### Atrous Spatial Pyramid Pooling (ASPP):

```python
# DeepLab ASPP: несколько dilated conv параллельно
aspp_branches = [
    Conv2d(C, 256, 1),                # d=1
    Conv2d(C, 256, 3, dilation=6),    # d=6
    Conv2d(C, 256, 3, dilation=12),   # d=12
    Conv2d(C, 256, 3, dilation=18),   # d=18
    GlobalAvgPool → Conv1x1,          # image-level
]
# Concatenate → Conv1x1 → выход
```

---

## 6. Group Convolution

Разделение каналов на **группы**, каждая сворачивается независимо:

$$\text{Input: } C_{\text{in}} = C_{\text{out}} = C$$

$$G \text{ групп, каждая размером } C/G$$

$$\text{Params: } k \cdot k \cdot \frac{C}{G} \cdot \frac{C}{G} \cdot G = \frac{k^2 C^2}{G}$$

### Сравнение:

| Число групп | Параметров | Связь между каналами |
|---|---|---|
| $G=1$ (стандартная) | $k^2 C^2$ | Полная |
| $G=C_{\text{in}}$ (depthwise) | $k^2 C$ | Нет (каждый отдельно) |
| $G=2$ | $\frac{k^2 C^2}{2}$ | Внутри группы |
| $G=32$ (ResNeXt) | $\frac{k^2 C^2}{32}$ | Внутри группы |

```python
# Group Convolution
nn.Conv2d(C, C, 3, groups=32, padding=1)
```

### Где используется:
- **ResNeXt**: $G=32$, кардинальность вместо ширины
- **Depthwise Conv**: частный случай $G = C_{\text{in}}$
- **ShuffleNet**: group conv + channel shuffle
- **Распараллеливание**: каждая группа на своём GPU

---

## 7. Сводная таблица видов свёрток

| Вид | Ядро | Параметров | RF | Размер выхода | Применение |
|---|---|---|---|---|---|
| **Standard** | $k \times k$ | $k^2 C_{\text{in}} C_{\text{out}}$ | $k$ | $H' < H$ | Базовая |
| **1×1 (Pointwise)** | $1 \times 1$ | $C_{\text{in}} C_{\text{out}}$ | 1 | $H' = H$ | Bottleneck, смешивание каналов |
| **Depthwise** | $k \times k$ | $k^2 C_{\text{in}}$ | $k$ | $H' < H$ | Пространственная, без channel mixing |
| **Depthwise Separable** | $k \times k + 1\times 1$ | $k^2 C_{\text{in}} + C_{\text{in}} C_{\text{out}}$ | $k$ | $H' < H$ | MobileNet, EfficientNet |
| **Transposed** | $k \times k$ | $k^2 C_{\text{in}} C_{\text{out}}$ | $k$ | $H' > H$ | Upsampling, U-Net |
| **Dilated** | $k \times k$ | $k^2 C_{\text{in}} C_{\text{out}}$ | $k_{\text{eff}} = k + (k-1)(d-1)$ | $H' < H$ | Segmentation, WaveNet |
| **Group** | $k \times k$ | $\frac{k^2 C_{\text{in}} C_{\text{out}}}{G}$ | $k$ | $H' < H$ | ResNeXt, ShuffleNet |

---

## 8. Пример: все виды свёрток в PyTorch

```python
import torch.nn as nn

# Standard
nn.Conv2d(64, 128, 3, padding=1, dilation=1)

# 1×1 (Pointwise)
nn.Conv2d(64, 256, 1)                    # squeeze/expand channels

# Depthwise (каждая группа = канал)
nn.Conv2d(64, 64, 3, groups=64, padding=1)

# Depthwise Separable (вручную)
class DSConv(nn.Module):
    def __init__(self, in_ch, out_ch):
        super().__init__()
        self.dw = nn.Conv2d(in_ch, in_ch, 3, groups=in_ch, padding=1)
        self.pw = nn.Conv2d(in_ch, out_ch, 1)

# Transposed (upsampling)
nn.ConvTranspose2d(256, 128, 4, stride=2, padding=1)

# Dilated
nn.Conv2d(64, 64, 3, dilation=2, padding=2)

# Group
nn.Conv2d(256, 256, 3, groups=32, padding=1)
```

---

## 9. Выбор вида свёртки — практические рекомендации

| Задача | Рекомендуемая свёртка | Почему |
|---|---|---|
| **Базовая CNN** | Standard 3×3 | Простота, эффективность |
| **Мобильные модели** | Depthwise Separable | 9× меньше параметров |
| **Bottleneck** | 1×1 → 3×3 → 1×1 | Экономия (ResNet) |
| **Высокое разрешение** | Dilated (d=2..4) | Большой RF без downsampling |
| **Сегментация (U-Net)** | Transposed + Skip | Восстановление разрешения |
| **Multi-scale context** | ASPP (Dilated) | Разные масштабы в 1 слое |
| **ResNeXt / Wide nets** | Group Conv (G=32) | Кардинальность |
| **Channel mixing только** | 1×1 | Без изменения H×W |

---

## 10. Резюме

- **1×1 Conv**: смешивание каналов, bottleneck, без изменения пространства.
- **Depthwise Separable**: разложение на пространственную + канальную свёртки (×9 экономия).
- **Transposed Conv**: upsampling (восстановление разрешения; осторожно с артефактами).
- **Dilated Conv**: большой receptive field без downsampling (сегментация).
- **Group Conv**: деление каналов на группы (ResNeXt, depthwise как частный случай).
- **Выбор зависит от задачи**: экономия, RF, разрешение или контекст.

---

**Связанные вопросы:**
- [[DL 10 - Операция свертки]] — базовая операция свёртки, параметры, receptive field
- [[DL 11 - Базовая CNN]] — применение свёрток в CNN для классификации
- [[DL 13 - Классические CNN-архитектуры]] — как разные свёртки используются в архитектурах
- [[DL 14 - ResNet]] — bottleneck с 1×1 свёртками, skip connections
