# DL 02 — Нейронная сеть как композиция функций

## 1. Композиция функций — фундаментальная идея

Нейронная сеть — это **композиция** (последовательное применение) большого количества простых параметрических функций:

$$f(x) = f_L \circ f_{L-1} \circ \ldots \circ f_1(x)$$

где $f_i$ — слой сети, $L$ — количество слоёв (глубина). Каждый слой — дифференцируемая функция, параметры которой обучаются через [[DL 04 - Backpropagation и autodiff|backpropagation]].

Каждый слой можно записать как:

$$h^{(l)} = \sigma(W^{(l)} h^{(l-1)} + b^{(l)})$$

где $h^{(0)} = x$ — входные данные, $h^{(L)} = \hat{y}$ — предсказание.

---

## 2. Линейный (полносвязный) слой — Linear / Fully Connected

### Определение
Линейный слой преобразует вектор входа $x \in \mathbb{R}^{d_{in}}$ в вектор выхода $y \in \mathbb{R}^{d_{out}}$:

$$y = W x + b$$

где:
- $W \in \mathbb{R}^{d_{out} \times d_{in}}$ — матрица весов (параметры)
- $b \in \mathbb{R}^{d_{out}}$ — вектор смещения (bias)

### Почему bias важен?
Без bias гиперплоскость разделения всегда проходит через начало координат:

$$y = Wx \quad\Rightarrow\quad Wx = 0$$

Bias добавляет сдвиг:

$$Wx + b = 0$$

### Вычислительно
Линейный слой — это **умножение матрицы на вектор** (или матрицы на матрицу для батча), что эффективно реализуется через BLAS/CUDA.

---

## 3. Функции активации — источник нелинейности

Композиция линейных слоёв без активаций — всё ещё линейная операция:

$$W_2(W_1 x + b_1) + b_2 = (W_2 W_1)x + (W_2 b_1 + b_2)$$

Нелинейная активация $\sigma(\cdot)$ между слоями делает сеть способной аппроксимировать любые функции ([[DL 03 - Обучение нейронной сети|Universal Approximation Theorem]]).

### Популярные функции активации

| Функция | Формула | Производная | Плюсы | Минусы |
|---|---|---|---|---|
| **Sigmoid** | $\sigma(x) = \frac{1}{1+e^{-x}}$ | $\sigma(x)(1-\sigma(x))$ | Гладкая, вероятностная интерпретация | Исчезающий градиент, не zero-centered |
| **Tanh** | $\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}$ | $1 - \tanh^2(x)$ | Zero-centered | Исчезающий градиент |
| **ReLU** | $\text{ReLU}(x) = \max(0, x)$ | $1 \text{ if } x>0, 0 \text{ if } x<0$ | Нет насыщения для $x>0$, дешёвая | "Умирающий ReLU" |
| **Leaky ReLU** | $\text{LReLU}(x) = \max(\alpha x, x)$ | $\alpha \text{ if } x<0, 1 \text{ if } x>0$ | Нет умирающих нейронов | Доп. гиперпараметр $\alpha$ |
| **ELU** | $\text{ELU}(x) = x \text{ if } x>0, \alpha(e^x-1) \text{ if } x \leq 0$ | — | Плавная около нуля | Вычислительно дороже |
| **GELU** | $\text{GELU}(x) = x \cdot \Phi(x)$ | — | SOTA для Transformer (GPT, BERT) | Сложная |
| **Swish / SiLU** | $\text{Swish}(x) = x \cdot \sigma(x)$ | — | Гладкая, саморегулируемая | Сложная |
| **Softmax** | $\text{softmax}(x_i) = \frac{e^{x_i}}{\sum_j e^{x_j}}$ | — | Вероятностное распределение | Используется на выходе |

### Ключевой момент: нелинейность
Глубокая сеть с нелинейными активациями — **универсальный аппроксиматор** (Cybenko, 1989):
> Любая непрерывная функция на компакте может быть аппроксимирована нейросетью с одним скрытым слоем (достаточно широким).

На практике **глубина** (много слоёв) эффективнее ширины (один широкий слой) для той же ёмкости модели.

---

## 4. Глубина (Depth)

### Почему глубина важна?

1. **Иерархия признаков**: каждый слой учит более абстрактное представление.
2. **Параметрическая эффективность**: глубокая сеть с $K$ слоями по $M$ нейронов требует меньше параметров, чем один широкий слой с тем же числом нейронов.
3. **Reuse признаков**: низкоуровневые признаки (рёбра, углы) переиспользуются в разных высокоуровневых концепциях.

### Проблемы глубины:
- [[DL 06 - Стабилизация и регуляризация|Исчезающий/взрывающийся градиент]]
- [[DL 14 - ResNet|Degradation]] (рост ошибки при добавлении слоёв; решается ResNet)
- Вычислительная сложность
- [[DL 08 - Диагностика обучения|Переобучение]]

---

## 5. Softmax — выходной слой для классификации

Softmax преобразует произвольный вектор логитов $z \in \mathbb{R}^K$ в вероятностное распределение:

$$p_i = \frac{e^{z_i}}{\sum_{j=1}^{K} e^{z_j}}$$

Свойства:
- $\sum_{i=1}^{K} p_i = 1$, $p_i > 0$
- Экспонента усиливает большие значения, подавляет маленькие
- Функция **не зависит от абсолютной величины** логитов (только от разницы)

### Численная стабильность
На практике вычитают максимум:

$$\text{softmax}(z_i) = \frac{e^{z_i - \max(z)}}{\sum_j e^{z_j - \max(z)}}$$

---

## 6. Полная архитектура сети (пример MLP)

### 2-слойный MLP для бинарной классификации:

$$h_1 = \text{ReLU}(W_1 x + b_1)$$
$$h_2 = \text{ReLU}(W_2 h_1 + b_2)$$
$$\hat{y} = \sigma(W_3 h_2 + b_3)$$

где $\sigma$ — сигмоида (бинарная) или softmax (многоклассовая).

### Иллюстрация композиции:

$$f(x) = \sigma \circ (W_3 \cdot (\text{ReLU} \circ (W_2 \cdot (\text{ReLU} \circ (W_1 x + b_1)) + b_2)) + b_3)$$

---

## 7. Сравнение: мелкая vs глубокая сеть

| Характеристика | Мелкая (1-2 скрытых слоя) | Глубокая (10+ слоёв) |
|---|---|---|
| **Выразительность** | Ограничена | Высокая |
| **Параметры на задачу** | Много | Относительно мало |
| **Обучение** | Проще (нет VGP) | Сложно (нужны Residual, BatchNorm) |
| **Transfer learning** | Плохо | Отлично |
| **Пример** | Logistic Regression + 1 слой | ResNet-152 (152 слоя) |

---

## 8. Резюме

- Нейронная сеть = **композиция функций**: $f(x) = f_L \circ ... \circ f_1(x)$
- **Линейный слой** $y = Wx + b$ — основа, bias обязателен
- **Функции активации** вносят нелинейность (ReLU — стандарт, GELU — SOTA)
- **Глубина** важнее ширины (иерархия, параметрическая эффективность)
- **Softmax** — стандартный выход для классификации
- Universal Approximation Theorem: один скрытый слой достаточен теоретически, но глубина практичнее

---

**Связанные вопросы:**
- [[DL 01 - Введение в Deep Learning]] — фундаментальные идеи DL
- [[DL 03 - Обучение нейронной сети]] — loss функции, ERM, mini-batch training
- [[DL 04 - Backpropagation и autodiff]] — обратное распространение для обучения композиции
- [[DL 07 - PyTorch пайплайн]] — реализация линейных слоёв и активаций в PyTorch
