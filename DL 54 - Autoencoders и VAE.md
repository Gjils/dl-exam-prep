# 54. Autoencoders и VAE

Генеративные модели-альтернативы: [[DL 52 - GAN]], [[DL 53 - Варианты и стабилизация GAN|варианты GAN]], [[DL 58 - Диффузионные модели|диффузионные модели]].

## 1. Autoencoder (AE)

**Autoencoder** — нейронная сеть, обучаемая восстанавливать свой вход. Состоит из двух частей:

1. **Encoder**: $q_\phi(z|x)$ — сжимает вход $x$ в скрытое представление $z$
2. **Decoder**: $p_\theta(x|z)$ — восстанавливает $x$ из $z$

### Архитектура:

```
x → [Encoder] → z → [Decoder] → x̂
```

### Функция потерь (Reconstruction loss):

Для непрерывных данных (MSE):
$$L_{AE} = \|x - \hat{x}\|_2^2 = \|x - D_\theta(E_\phi(x))\|_2^2$$

Для бинарных данных (binary cross-entropy):
$$L_{AE} = -\sum_i [x_i \log \hat{x}_i + (1 - x_i) \log (1 - \hat{x}_i)]$$

### Bottleneck

**Bottleneck** — центральный слой с малой размерностью ($\dim(z) \ll \dim(x)$). Он вынуждает модель находить наиболее информативные признаки.

- **Undercomplete AE**: $\dim(z) < \dim(x)$ — учит сжатое представление
- **Overcomplete AE**: $\dim(z) > \dim(x)$ — без регуляризации может выучить identity

### Latent Space

Скрытое пространство $z$ — это многообразие, в которое проецируются входные данные. Свойства:
- Неструктурированное (плохая интерполяция)
- Непрерывное, но не обязательно полное без пробелов
- Точки могут быть неинтерпретируемы

### Варианты Autoencoder

| Тип | Описание | Применение |
|---|---|---|
| **Denoising AE** | Вход зашумлён, выход — чистый | Очистка, устойчивые признаки |
| **Sparse AE** | L1-регуляризация на $z$ | Выявление редких признаков |
| **Contractive AE** | Штраф на $\|\nabla_x z\|$ | Устойчивость к малым изменениям |
| **Stacked AE** | Многослойный | Глубокие представления |

### Ограничения AE:

- Нет вероятностной интерпретации
- Лакуны в латентном пространстве (плохая генерация новых точек)
- Нет гарантии, что $z$ следует простому распределению

## 2. Variational Autoencoder (VAE)

**VAE** (Kingma & Welling, 2013) — вероятностная версия autoencoder. Вместо детерминированного $z$ кодируется **распределение** $q_\phi(z|x)$. Развитие идеи VAE с дискретным латентным пространством даёт [[DL 55 - Normalizing Flows и VQ-VAE|VQ-VAE]].

### Идея:

VAE моделирует $p_\theta(x)$ — вероятность данных. Для этого вводится латентная переменная $z$:

$$p_\theta(x) = \int p_\theta(x|z) p(z) dz$$

Прямое вычисление интеграла невозможно. Используем **Variational Inference** — аппроксимируем истинную апостериорную $p_\theta(z|x)$ вариационным распределением $q_\phi(z|x)$ (encoder).

### ELBO (Evidence Lower Bound)

Логарифм правдоподобия разлагается:

$$\log p_\theta(x) = \underbrace{\mathbb{E}_{z \sim q_\phi} \left[\log \frac{p_\theta(x, z)}{q_\phi(z|x)}\right]}_{\text{ELBO}} + \underbrace{KL(q_\phi(z|x) \parallel p_\theta(z|x))}_{\geq 0}$$

Отсюда:

$$\log p_\theta(x) \geq \text{ELBO}(\theta, \phi)$$

### Формула ELBO:

$$\text{ELBO}(\theta, \phi) = \underbrace{\mathbb{E}_{z \sim q_\phi(z|x)} [\log p_\theta(x|z)]}_{\text{Reconstruction term}} - \underbrace{KL(q_\phi(z|x) \parallel p(z))}_{\text{KL divergence}}$$

### Два члена ELBO:

1. **Reconstruction term**: ожидание логарифма правдоподобия — насколько хорошо $x$ восстанавливается из $z$
2. **KL divergence term**: штраф за отклонение апостериорного $q_\phi(z|x)$ от априорного $p(z)$ (обычно $\mathcal{N}(0, I)$)

### Развёрнутая форма:

$$\text{ELBO} = \mathbb{E}_{z \sim q_\phi} [\log p_\theta(x|z)] - \int q_\phi(z|x) \log \frac{q_\phi(z|x)}{p(z)} dz$$

Если $q_\phi(z|x) = \mathcal{N}(\mu_\phi(x), \sigma_\phi^2(x))$ и $p(z) = \mathcal{N}(0, I)$:

$$KL(q_\phi(z|x) \parallel p(z)) = -\frac{1}{2} \sum_{j=1}^J (1 + \log \sigma_j^2 - \mu_j^2 - \sigma_j^2)$$

где $J = \dim(z)$.

### Reparameterization Trick

**Проблема:** $z \sim \mathcal{N}(\mu, \sigma^2)$ — сэмплирование не дифференцируемо (нет градиента через случайность).

**Решение:** перепараметризация:

$$z = \mu + \sigma \odot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)$$

Теперь:
- $\epsilon$ — источник случайности (не обучается)
- $\mu, \sigma$ — обучаемые параметры
- Градиент проходит через $\mu$ и $\sigma$ к encoder

### Алгоритм обучения VAE:

1. Encoder выдаёт $\mu_\phi(x)$ и $\log\sigma_\phi^2(x)$
2. Сэмплируем $\epsilon \sim \mathcal{N}(0, I)$
3. Вычисляем $z = \mu + \sigma \odot \epsilon$
4. Decoder восстанавливает $\hat{x} = p_\theta(x|z)$
5. Вычисляем ELBO: reconstruction loss + KL divergence
6. Градиентный шаг по $\theta, \phi$

## 3. VAE vs AE — сравнение

| Характеристика | AE | VAE |
|---|---|---|
| **Латентное представление** | Детерминированное $z = E(x)$ | Вероятностное $z \sim q_\phi(z\|x)$ |
| **Априорное распределение** | Нет | $p(z) = \mathcal{N}(0, I)$ (обычно) |
| **Latent space** | Неструктурирован, лакуны | Структурирован, непрерывен |
| **Генерация новых точек** | Не гарантирована (лакуны) | ✓ Работает (сэмпл $z \sim p(z)$) |
| **Интерполяция** | Плохая | ✓ Плавная |
| **Качество реконструкции** | ✓ Высокое (меньше ограничений) | Среднее (KL регуляризует) |
| **Функция потерь** | $\|x - \hat{x}\|^2$ | ELBO: rec + KL |
| **Интерпретируемость** | Низкая | Средняя (каждый канал $z$ может кодировать признак) |
| **Масштабируемость** | Высокая | Высокая |

### Торговля: Reconstruction vs Regularization

VAE балансирует между:
- **Высокое качество реконструкции** → малое KL (позволяет $z$ быть специфичным для каждого $x$)
- **Хорошая структура латентного пространства** → большое KL (заставляет $z$ следовать априорному распределению)

Этот trade-off можно регулировать:

$$\text{ELBO} = \mathbb{E}_{z \sim q_\phi} [\log p_\theta(x|z)] - \beta \cdot KL(q_\phi(z|x) \parallel p(z))$$

где $\beta$ — вес KL (в $\beta$-VAE).

### Применения VAE:

| Задача | Описание |
|---|---|
| **Генерация данных** | Сэмплирование $z \sim \mathcal{N}(0, I)$, декодирование |
| **Сглаживание/интерполяция** | Линейная интерполяция между $z_1$ и $z_2$ |
| **Anomaly detection** | Высокий reconstruction loss = аномалия |
| **Предобучение** | Инициализация encoder для downstream задач |
| **Disentangled representation** | $\beta$-VAE для разделения факторов вариаций |

### Ограничения VAE:

1. **Размытость генераций** — из-за гауссовского правдоподобия (MSE даёт среднее)
2. **Приближённая апостериорная** — $q_\phi(z\|x)$ может быть плохой аппроксимацией
3. **Posterior collapse** — при сильном decoder $q_\phi(z\|x)$ игнорирует $z$ (KL → 0)
---

**Связанные вопросы:** [[DL 52 - GAN]], [[DL 53 - Варианты и стабилизация GAN]], [[DL 55 - Normalizing Flows и VQ-VAE]], [[DL 58 - Диффузионные модели]]
