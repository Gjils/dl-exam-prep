# DL 47 — Частотное представление звука

> **Fourier transform, DFT/FFT, STFT, спектрограмма, mel-scale, log-mel features, MFCC.
> Смежные темы: [[DL 46 - Звук как сигнал]], [[DL 48 - ASR]], [[DL 49 - CTC]].**

---

## 1. Почему нужно частотное представление?

[[DL 46 - Звук как сигнал|Waveform]] (временная область) показывает, **когда** звук был громким, но не показывает **какие частоты** присутствуют.

Частотное представление показывает **какие частоты** и **с какой амплитудой** есть в сигнале.

---

## 2. Fourier Transform (преобразование Фурье)

### Непрерывное (FT)

Переводит сигнал из временной области в частотную:

$$
X(f) = \int_{-\infty}^{\infty} x(t) \cdot e^{-2\pi i f t} dt
$$

где $x(t)$ — сигнал во времени, $X(f)$ — спектр (комплексный).

**Обратное преобразование:**

$$
x(t) = \int_{-\infty}^{\infty} X(f) \cdot e^{2\pi i f t} df
$$

### Физический смысл

- $|X(f)|$ — **амплитудный спектр** (какая частота насколько громкая)
- $\angle X(f)$ — **фазовый спектр** (сдвиг фазы)

---

## 3. DFT (Discrete Fourier Transform)

Для дискретного сигнала $x[n]$ длины $N$:

$$
X[k] = \sum_{n=0}^{N-1} x[n] \cdot e^{-2\pi i k n / N}, \quad k = 0, 1, \dots, N-1
$$

**Свойства:**
- Вход: $N$ временных отсчётов
- Выход: $N$ комплексных частотных отсчётов
- Диапазон частот: $[0, f_s)$ (дискретных)
- $X[N-k] = \overline{X[k]}$ (симметрия для вещественного сигнала)

**Разрешение по частоте:**

$$
\Delta f = \frac{f_s}{N}
$$

**Пример:** $f_s = 16\text{ кГц}, N = 1024 \rightarrow \Delta f = 15.625\text{ Гц}$

---

## 4. FFT (Fast Fourier Transform)

**FFT** — алгоритмическая оптимизация вычисления DFT.

**Сложность:**

| Метод | Сложность |
|---|---|
| DFT (прямое вычисление) | $O(N^2)$ |
| FFT (Cooley-Tukey) | $O(N \log N)$ |

**Принцип FFT:**
- Разделяй и властвуй (divide and conquer)
- Разбиваем DFT размера $N$ на две DFT размера $N/2$
- Рекурсивно вычисляем
- Объединяем с "бабочками" (butterfly operations)

### Требование к $N$

Наиболее эффективен, когда $N$ — степень двойки: $2^k$.

**Пример:** $N = 2^{10} = 1024$ → 10 уровней рекурсии.

---

## 5. STFT (Short-Time Fourier Transform)

### Проблема FT

FT даёт **глобальный** спектр — какие частоты есть в сигнале, но не **когда** они появляются. Речь меняется во времени — нужно **частотно-временное** представление.

### Идея STFT

Разбить сигнал на **окна** (frames) и вычислить DFT для каждого окна:

$$
X[m, k] = \sum_{n=0}^{N-1} x[n + m \cdot H] \cdot w[n] \cdot e^{-2\pi i k n / N}
$$

где:
- $m$ — номер окна (frame index)
- $k$ — частота (frequency bin)
- $w[n]$ — оконная функция (window)
- $H$ — hop size (шаг между окнами)
- $N$ — размер окна

### Параметры STFT

| Параметр | Типичное значение | Эффект |
|---|---|---|
| **Window size ($N$)** | 20–30 мс (256–1024 сэмплов) | Разрешение по времени/частоте |
| **Hop size ($H$)** | 10–15 мс (50% overlap) | Плавность спектрограммы |
| **Window function** | Hann / Hamming | Подавление боковых лепестков |

### Time-Frequency trade-off

> **Heisenberg-Gabor limit:** невозможно одновременно получить высокое разрешение и по времени, и по частоте.

- Большое окно → хорошее частотное разрешение, плохое временное
- Малое окно → хорошее временное, плохое частотное

### Оконные функции

| Функция | Формула | Боковые лепестки |
|---|---|---|
| Rectangular | $w[n] = 1$ | Высокие (-13 dB) |
| Hann | $w[n] = 0.5(1 - \cos(2\pi n/N))$ | -31 dB |
| Hamming | $w[n] = 0.54 - 0.46\cos(2\pi n/N)$ | -41 dB |
| Blackman | $w[n] = 0.42 - 0.5\cos(2\pi n/N) + 0.08\cos(4\pi n/N)$ | -57 dB |

---

## 6. Спектрограмма (Spectrogram)

### Определение

**Spectrogram** — визуализация STFT: время (x), частота (y), амплитуда (цвет/яркость).

### Стандартные типы

| Тип | Значение цвета | Формула |
|---|---|---|
| **Power spectrogram** | Энергия | $|X[m, k]|^2$ |
| **Magnitude spectrogram** | Амплитуда | $|X[m, k]|$ |
| **Log-power spectrogram** | dB | $10 \log_{10} |X[m, k]|^2$ |

### Пример структуры

```
Frequency
  ↑
 8кГц ┼───░▒▓░░▓▒░────────────────
      │   ░▓█████▓░───░░░░────────
 4кГц ┼──░▓██████▓░──░▓▓▓░───────
      │  ░▓██░▒▓▓▓░──░▓██▓░───────
 2кГц ┼─░▓██░░░▒░░──░▓██▓░───────
      │ ░▓██░░░░░░──░▓██▓░░░▓░───
 1кГц ┼▓▓██▓░░░░░──░▓██▓░░░▓▓░──
      └───────────────────────────▶ Time
      0мс    200мс    400мс    600мс
```

---

## 7. Mel-Scale

### Психологическая мотивация

Человеческое восприятие высоты звука **нелинейно**:
- **Низкие частоты** (< 1 кГц): различаем хорошо
- **Высокие частоты** (> 1 кГц): различаем хуже

### Формула mel-scale

$$
m = 2595 \cdot \log_{10}\left(1 + \frac{f}{700}\right)
$$

**Обратное преобразование:**

$$
f = 700 \cdot (10^{m/2595} - 1)
$$

### Сравнение

| $f$ (Гц) | $m$ (mel) |
|---|---|
| 100 | 159 |
| 500 | 696 |
| 1000 | 1000 |
| 2000 | 1467 |
| 4000 | 2019 |
| 8000 | 2668 |

### Mel Filterbank

Набор треугольных фильтров, равномерно расположенных в mel-шкале:

- $N$ фильтров (обычно 40–128)
- Фильтр $i$: пик в центре, линейный спад до соседей
- Умножаем спектр $|X|^2$ на фильтры → энергия в каждом mel-канале

---

## 8. Log-Mel Features (Log Mel-Spectrogram)

### Получение

```
Waveform → STFT → Mel Filterbank → Log ∘ Power
```

1. STFT получает спектр
2. Mel filterbank сжимает частоты по mel-шкале
3. Логарифмирование: $\log(|X_{\text{mel}}|^2)$

### Почему логарифм?

- Амплитуды в dB $\propto \log(\text{power})$
- Громкость воспринимается логарифмически (закон Вебера-Фехнера)
- Сжатие динамического диапазона

### Использование в Deep Learning

**Log-Mel spectrogram** — наиболее распространённый вход для нейросетевых [[DL 48 - ASR|ASR]]/TTS систем:

| Система | Вход |
|---|---|
| DeepSpeech | Log-Mel (спектрограмма) |
| Jasper, QuartzNet | Log-Mel |
| [[DL 48 - ASR|Whisper]] | Log-Mel (80 каналов) |
| Tacotron (TTS) | Log-Mel (80 каналов) |

**Типичный вход:** $80 \times T$ (80 mel-каналов × T фреймов), 16 кГц, window 25 мс, hop 10 мс.

---

## 9. MFCC (Mel-Frequency Cepstral Coefficients)

### Что такое Cepstrum

"Спектр спектра" — обратный спектр log-спектра:

$$
\text{cepstrum} = \mathcal{F}^{-1}\{\log(|\mathcal{F}\{x(t)\}|^2)\}
$$

**Cepstral domain:** разделение источника (source) и фильтра (vocal tract).

### Получение MFCC

```
Log-Mel → DCT (Discrete Cosine Transform) → Select first M coefficients
```

**Полный пайплайн:**

```
1. Waveform
2. Pre-emphasis (усиление высоких частот): y[t] = x[t] - α·x[t-1], α=0.97
3. Frame blocking (25 мс, hop 10 мс)
4. Windowing (Hamming)
5. FFT
6. Mel filterbank (обычно 26–40 фильтров)
7. Log(|⋅|²)
8. DCT → первые 13 MFCC + Δ + ΔΔ
```

**Формула DCT для MFCC:**

$$
c_i = \sum_{j=1}^{M} E_j \cdot \cos\left(\frac{\pi i}{M}(j - 0.5)\right), \quad i = 0, 1, \dots, L-1
$$

где $E_j$ — log-энергия в $j$-м mel-канале.

### Delta (Δ) и Delta-Delta (ΔΔ)

Добавляют динамику — изменение коэффициентов во времени:

$$
\Delta c_t = \frac{c_{t+1} - c_{t-1}}{2} \quad \text{(первые производные)}
$$

$$
\Delta\Delta c_t = \frac{\Delta c_{t+1} - \Delta c_{t-1}}{2} \quad \text{(вторые производные)}
$$

**Итоговый вектор:** 13 MFCC + 13 Δ + 13 ΔΔ = **39 признаков**

### MFCC vs Log-Mel для Deep Learning

| Аспект | Log-Mel | MFCC |
|---|---|---|
| Информация | Полная (вся энергия по каналам) | Сжатая (DCT до 13–40 коэфф.) |
| Корреляция каналов | Высокая | Низкая (DCT декоррелирует) |
| Размерность | 80–128 | 13–40 (+Δ, ΔΔ) |
| Для нейросетей | Лучше (CNN любят корреляции) | Хуже (потеря информации) |
| Для GMM/HMM | Хуже | Лучше (декорреляция) |
| Современные ASR | Стандарт (Whisper, Wav2Vec2) | Устаревший (традиционный) |

---

## 10. Сравнение всех представлений

| Представление | Время | Частота | Информация | Размер | Использование |
|---|---|---|---|---|---|
| Waveform | ✅ | ❌ | Полная | $T$ | Raw audio |
| STFT (спектр) | ✅ | ✅ | Полная | $F \times T$ | Обработка |
| Power spectrogram | ✅ | ✅ | Полная (без фазы) | $F \times T$ | Визуализация |
| Log-Mel | ✅ | ✅ (mel) | Частичная | $M \times T$ | Deep Learning |
| MFCC | ✅ | ✅ (mel+DCT) | Сжатая | $M' \times T$ | Классические методы |
| Cepstrum | ✅ (quefrency) | ❌ | Огибающая/детали | $N$ | Pitch, форманты |

---

## 11. Ключевые выводы

1. **FT** переводит сигнал из времени в частоту, **STFT** — для анализа изменения во времени
2. **FFT** — $O(N\log N)$ реализация DFT — основа всей цифровой обработки звука
3. **Спектрограмма** — визуализация STFT, основной вход для аудио-нейросетей
4. **Mel-scale** — нелинейная шкала частот, соответствующая человеческому слуху
5. **Log-Mel** — современный стандарт для [[DL 48 - ASR|ASR]]/TTS (80 каналов, 16 кГц, 25 мс окно)
6. **MFCC** — классический признак (DCT сжатие log-mel); уступает log-mel для нейросетей
7. **Log-Mel > MFCC** для современных глубоких моделей благодаря сохранению пространственной структуры

---

**Связанные вопросы:**
- [[DL 46 - Звук как сигнал]] — основы цифрового аудио, дискретизация, PCM
- [[DL 48 - ASR]] — распознавание речи на основе log-mel спектрограмм
- [[DL 49 - CTC]] — декодирование аудио-признаков в текст
