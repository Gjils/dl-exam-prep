# 56. Графы как данные и GNN

## 1. Графы как данные

**Граф** $\mathcal{G} = (V, E)$ — множество вершин $V$ ($|V| = n$) и рёбер $E \subseteq V \times V$. В отличие от [[DL 09 - Изображение как тензор, мотивация CNN|изображений]] (регулярная сетка пикселей) или [[DL 36 - GPT|текстовых последовательностей]], графы не имеют естественного порядка элементов.

### Способы представления графов:

| Представление | Описание |
|---|---|
| **Матрица смежности** | $A \in \mathbb{R}^{n \times n}$, $A_{ij} = 1$ если $(i, j) \in E$, иначе 0 |
| **Список смежности** | Для каждой вершины список соседей |
| **Матрица Лапласа** | $L = D - A$, где $D = diag(d_1, \dots, d_n)$ |
| **Нормализованная Лапласа** | $\hat{L} = I - D^{-1/2} A D^{-1/2}$ |

### Данные на графах:

- **Feature matrix**: $X \in \mathbb{R}^{n \times d}$ — признаки вершин
- **Edge features**: $e_{ij} \in \mathbb{R}^{d_e}$ — признаки рёбер
- **Global features**: $u \in \mathbb{R}^{d_g}$ — глобальные признаки графа

### Свойства графовых данных:
- **Неевклидовость** — нет естественной сетки/координат
- **Неупорядоченность** — вершины не имеют фиксированного порядка
- **Отношения** — информация в связях не менее важна, чем в признаках
- **Инвариантность к перестановкам** — перестановка вершин не меняет граф

## 2. Задачи на графах

### Node-level задачи

Цель: предсказать свойство каждой вершины.

| Задача | Пример |
|---|---|
| **Node classification** | Классификация темы статьи по цитированиям |
| **Node regression** | Предсказание трафика для узла сети |
| **Node clustering** | Выявление сообществ |

**Формально:** $f: (A, X, v_i) \to y_i$

### Edge-level задачи

Цель: предсказать свойство связи между вершинами.

| Задача | Пример |
|---|---|
| **Link prediction** | Предсказание новых друзей в соцсети |
| **Edge classification** | Определение типа связи (друг/родственник) |
| **Relation prediction** | Заполнение недостающих фактов в KG |

**Формально:** $f: (A, X, v_i, v_j) \to y_{ij}$

### Graph-level задачи

Цель: предсказать свойство всего графа.

| Задача | Пример |
|---|---|
| **Graph classification** | Классификация молекулы по токсичности |
| **Graph regression** | Предсказание энергии молекулы |
| **Graph generation** | Генерация новых молекул |

**Формально:** $f: (A, X, \mathcal{G}) \to y_\mathcal{G}$

## 3. Message Passing — основная парадигма GNN

**Message Passing** — общий механизм обновления представлений вершин путём агрегации сообщений от соседей.

### Общая схема:

Для вершины $v$ на $k$-м слое:

$$h_v^{(k)} = \text{UPDATE}^{(k)} \left( h_v^{(k-1)}, \text{AGGREGATE}^{(k)} \left( \{ h_u^{(k-1)} : u \in \mathcal{N}(v) \} \right) \right)$$

где:
- $h_v^{(0)} = x_v$ — начальные признаки вершины
- $\mathcal{N}(v)$ — соседи вершины $v$
- **AGGREGATE** — функция агрегации (сумма, среднее, максимум, внимание)
- **UPDATE** — функция обновления (обычно MLP)

### Универсальный Message Passing:

$$m_v^{(k)} = \sum_{u \in \mathcal{N}(v)} M_k(h_v^{(k-1)}, h_u^{(k-1)}, e_{vu})$$
$$h_v^{(k)} = U_k(h_v^{(k-1)}, m_v^{(k)})$$

### Свойства Message Passing:
- **Инвариантность к перестановкам** (permutation invariance) — агрегация симметрична
- **Локальность** — каждый слой учитывает 1-hop соседей
- **Масштабируемость** — линейная по числу рёбер $O(|E|)$

## 4. Архитектуры GNN

### GCN (Graph Convolutional Network)

**GCN** (Kipf & Welling, 2017) — приближение спектральной свёртки первого порядка.

**Пропагация:**

$$h_v^{(k)} = \sigma \left( W^{(k)} \sum_{u \in \mathcal{N}(v) \cup \{v\}} \frac{1}{\sqrt{d_v d_u}} h_u^{(k-1)} \right)$$

В матричной форме:

$$H^{(k)} = \sigma \left( \tilde{D}^{-1/2} \tilde{A} \tilde{D}^{-1/2} H^{(k-1)} W^{(k)} \right)$$

где $\tilde{A} = A + I$ (self-loops), $\tilde{D}$ — степень $\tilde{A}$.

**Агрегация:** взвешенное среднее (нормализованное степенью).
**Особенность:** новая вершина может быть вычислена без переобучения (inductive).

### GraphSAGE (Sample and Aggregate)

**GraphSAGE** (Hamilton et al., 2017) — индуктивный метод для больших графов.

**Идея:** сэмплирование фиксированного числа соседей (а не всех).

$$h_v^{(k)} = \sigma \left( W^{(k)} \cdot \text{CONCAT} \left( h_v^{(k-1)}, \text{AGG} \left( \{ h_u^{(k-1)} : u \in \mathcal{N}_{sample}(v) \} \right) \right) \right)$$

**Варианты агрегации:**

| Агрегатор | Формула | Свойства |
|---|---|---|
| **Mean** | $\frac{1}{|\mathcal{N}|} \sum_{u \in \mathcal{N}} h_u$ | Линейная, сглаживает |
| **Pool** | $\max(\{ \sigma(Wh_u + b) : u \in \mathcal{N} \})$ | Нелинейная |
| **LSTM** | LSTM по перемешанным соседям | Не симметрична (требует перемешивания) |
| **GCN** | Взвешенное среднее со степенью | Теоретически обоснована |

**Преимущества GraphSAGE:**
- Работает с любыми графами (inductive)
- Сэмплирование фиксированного размера для обучения на больших графах
- Гибкие агрегаторы

### GAT (Graph Attention Network)

**GAT** (Veličković et al., 2018) — [[DL 33 - Архитектура Transformer|внимание]] на графах.

**Коэффициенты внимания:**

$$e_{vu} = a(W h_v, W h_u) = \text{LeakyReLU} \left( a^\top [W h_v \parallel W h_u] \right)$$

**Нормализация через softmax:**

$$\alpha_{vu} = \text{softmax}_u(e_{vu}) = \frac{\exp(e_{vu})}{\sum_{k \in \mathcal{N}(v)} \exp(e_{vk})}$$

**Обновление:**

$$h_v' = \sigma \left( \sum_{u \in \mathcal{N}(v)} \alpha_{vu} W h_u \right)$$

**Multi-head attention:**

$$h_v' = \parallel_{m=1}^M \sigma \left( \sum_{u \in \mathcal{N}(v)} \alpha_{vu}^{(m)} W^{(m)} h_u \right)$$

**Преимущества GAT:**
- Разные веса для разных соседей (адаптивно)
- Интерпретируемость (какие соседи важны)
- Не требует дорогой спектральной факторизации

## 5. Сравнение архитектур GNN

| Характеристика | GCN | GraphSAGE | GAT |
|---|---|---|---|
| **Агрегация** | Нормализованное среднее | Сэмплирование + агрегация | Взвешенная (внимание) |
| **Weight sharing** | Фиксированные веса | Фиксированные веса | Адаптивные веса |
| **Вычислительная сложность** | $O(\|E\| D)$ | $O(\|V\| SD)$ | $O(\|E\| D)$ (с attention) |
| **Inductive** | Да | ✓ Да | ✓ Да |
| **Big graphs** | Средне | ✓ Хорошо (сэмплирование) | Средне |
| **Интерпретируемость** | Низкая | Низкая | ✓ Высокая (attention) |
| **Базовый метод** | 2017 | 2017 | 2018 |

### Необходимость в сэмплировании

При обучении на больших графах (Facebook, Twitter) полный граф не помещается в память. Решения:
1. **GraphSAGE**: фиксированное число соседей на каждом уровне
2. **Cluster-GCN**: разбиение графа на кластеры
3. **GraphSAINT**: сэмплирование подграфа

## 6. Graph-level через Pooling

Для graph-level задач нужно получить одно представление всего графа из представлений вершин.

**Методы:**

| Метод | Описание |
|---|---|
| **Global mean/sum** | $\sum_{v} h_v$, $\frac{1}{n}\sum_v h_v$ |
| **Global max** | $\max_v h_v$ |
| **SortPool** | Сортировка вершин + свёртка (Zhang et al., 2018) |
| **DiffPool** | Обучаемая иерархическая кластеризация (Ying et al., 2018) |
| **SAGPool** | Self-attention pooling |
| **Virtual node** | Добавление виртуальной вершины, соединённой со всеми |

### Readout function:

$$h_\mathcal{G} = \text{READOUT}(\{h_v^{(K)} : v \in V\})$$

Обычно READOUT — permutation-invariant функция (сумма/среднее/максимум).

---

**Связанные вопросы:** [[DL 57 - Проблемы и современные направления GNN]], [[DL 09 - Изображение как тензор, мотивация CNN]], [[DL 33 - Архитектура Transformer]]
